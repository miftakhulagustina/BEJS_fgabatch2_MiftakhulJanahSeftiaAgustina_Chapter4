const express = require('express');
const router = express.Router();
//const prisma = require('../db/prisma');

const ACCOUNTS_CONTROLLER = require('../controller/account-controller');

module.exports = router;
